const app = Vue.createApp({
    data() {
        return {
            product: "Socks",
            description:
                "Super sexy socks available in two eye-catching colors!",
        };
    },
});
